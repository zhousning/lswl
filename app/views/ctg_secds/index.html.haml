- provide(:title, t('.title'))


.container-fluid
  .row
    = render "layouts/control_sidebar"
    %main.col-md-9.ml-sm-auto.col-lg-10.p-3{:role => "main"}
      - content_for :card_header do
        %h5
          = fa_icon "list", text: t(controller_name + "." + action_name + '.title')
          = link_to "/#{controller_name}/new", :class => "button pull-right button-primary" do
            = fa_icon "plus", text: Setting.systems.btn_new, right: false 
      - content_for :card_body do
        .container-fluid
          .row
            .col-md-12
              %table.text-center.table-striped.table-sm{:'data-toggle' => "table", :"data-pagination"=>"true", :'data-id-table'=>"advancedTable"}
                %thead
                  %tr
                    %th{:scope => "col"} #
                  
                    %th{:scope => "col"}
                      = Setting.ctg_secds.name
                    %th{:scope => "col"}
                      设置
                %tbody
                  - @ctg_secds.each_with_index do |ctg_secd, index|
                    %tr
                      %td{:scope => "row"}
                        = index+1
                    
                      %td
                        = ctg_secd.name
                      %td
                        .container-fluid
                          .row.justify-content-center
                            = link_to "编辑", edit_ctg_secd_path(ctg_secd), :class => "button button-highlight button-small mr-1" 
                            = link_to "详细",  ctg_secd_path(ctg_secd), :class => "button button-primary button-small mr-1" 
                            = link_to "删除",  ctg_secd, :method => :delete, :data => {:confirm => "确定删除吗?"}, :class => "button button-caution button-small" 
      = render "layouts/bootstrap/card"
