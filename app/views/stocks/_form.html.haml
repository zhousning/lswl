.container-fluid
  .row.justify-content-sm-center
    .col-sm-12
      = form_for @stock, :builder => WebsiteFormBuilder, :html => {:'data-parsley-validate' => true} do |f|
        - if @stock.errors.any?
          #error_explanation.alert.alert-danger.alert-dismissible.fade.show
            %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
              %span{"aria-hidden" => "true"} x 
            %h2= "#{pluralize(@stock.errors.count, "error")} prohibited this buyer from being saved:"
            %ul
              - @stock.errors.full_messages.each do |msg|
                %li= msg
      
        .form-group.row
          = f.label Setting.stocks.count, :class => "col-sm-3 col-form-label text-right"
          .col-sm-6
            = f.number_field :count, :class => "form-control rounded-0" , :required => true
          
        .form-group.row
          = f.label Setting.stocks.position, :class => "col-sm-3 col-form-label text-right"
          .col-sm-6
            = f.text_field :position, :class => "form-control rounded-0" 
          
      
      
      
      
      
        .form-group.row
          .col-3.offset-3
            = f.submit '保存', :class => "button button-action"
        -#
          .col-3.offset-1
            = link_to '返回', stocks_path, :class => "button button-caution"
